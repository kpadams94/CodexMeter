# Domain Docs

This is a single-context repository.

Before working, read `CONTEXT.md` and relevant decisions under `docs/adr/` when they exist. Proceed silently when they do not.

Use terminology defined in `CONTEXT.md`. Explicitly flag proposals that contradict an existing architectural decision.
